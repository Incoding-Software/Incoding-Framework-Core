the 2–3 most-used extension methods / base spec classes

the standard naming (When_..., Given_..., It_...)

how the container/dispatcher is bootstrapped in tests

one “golden” test (like Incoding.WebTest30.Tests.When_DoSomething) with brief commentary